f ← {y:
        + ← {x:n y:n "x+y":n}
        {+} y
}
#f

¬

f ← (
    a ← b + 5
~ a recebe de expr;       b recebe lazily de AST.m
    b ← 3
    a
    ff ← {a b c: c}
~ a,b,c tem hosh proprio            c tem próprio
    - ← {x:n y:n "x-y":n}
    + ← {x:n y:n "x+y":n}
    * ← {x:n y:n "x*y":n}
    / ← {x:n y:n "x/y":n}
    ^ ← {x:n y:n "scala.math.pow(x,y)":n}
    ação_2 ← {_1}
    g ← 4 + 8
    g


    x ←5
    d ← {_1}
)


~ f recebe do runtime.m

~ [] ← {x l:}
